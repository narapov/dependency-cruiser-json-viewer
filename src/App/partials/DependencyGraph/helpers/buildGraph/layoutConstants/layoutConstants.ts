/** Inner padding around children inside a folder group. */
export const GROUP_PADDING = 16;
/** Height reserved for the folder group header. */
export const GROUP_HEADER = 36;
/** Horizontal gap used when reflowing overlapping siblings. */
export const GRID_GAP_X = 60;
/** Vertical gap used when reflowing overlapping siblings. */
export const GRID_GAP_Y = 24;
